openapi: 3.0.4
info:
  title: Codemetrics API
  description: Backend for the Codemetrics Service.
  version: 2.40.2

paths:
  /api/analysis/code-hotspots:
    $ref: "./paths/api/analysis/code-hotspots.yaml"
  /api/workloads/{workloadId}/issue-types:
    $ref: "./paths/api/workloads/issue-types.yaml"

components:
  schemas:
    Error:
      $ref: "./components/schemas/Error.yaml"
