apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "code-metrics-promosite.fullname" . }}-test-connection"
  labels:
    {{- include "code-metrics-promosite.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "code-metrics-promosite.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
