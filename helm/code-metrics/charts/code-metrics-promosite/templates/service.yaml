apiVersion: v1
kind: Service
metadata:
  name: {{ include "code-metrics-promosite.fullname" . }}
  labels:
    {{- include "code-metrics-promosite.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "code-metrics-promosite.selectorLabels" . | nindent 4 }}
