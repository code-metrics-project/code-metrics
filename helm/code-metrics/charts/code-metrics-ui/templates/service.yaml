apiVersion: v1
kind: Service
metadata:
  name: {{ include "code-metrics-ui.fullname" . }}
  labels:
    {{- include "code-metrics-ui.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "code-metrics-ui.selectorLabels" . | nindent 4 }}
