apiVersion: v1
kind: Service
metadata:
  name: {{ include "mock-api.fullname" . }}
  labels:
    {{- include "mock-api.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "mock-api.selectorLabels" . | nindent 4 }}
