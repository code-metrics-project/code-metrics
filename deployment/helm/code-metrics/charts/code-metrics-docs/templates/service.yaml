apiVersion: v1
kind: Service
metadata:
  name: {{ include "code-metrics-docs.fullname" . }}
  labels:
    {{- include "code-metrics-docs.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "code-metrics-docs.selectorLabels" . | nindent 4 }}
